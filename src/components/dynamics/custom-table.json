{
  "collectionName": "components_dynamics_custom_tables",
  "info": {
    "displayName": "Custom Table",
    "icon": "table",
    "description": ""
  },
  "options": {},
  "attributes": {
    "table": {
      "type": "relation",
      "relation": "oneToOne",
      "target": "api::dynamic-table.dynamic-table"
    },
    "padding_top": {
      "type": "enumeration",
      "enum": [
        "none",
        "small",
        "medium",
        "large",
        "extra-large"
      ],
      "required": true,
      "default": "small"
    },
    "padding_bottom": {
      "type": "enumeration",
      "enum": [
        "none",
        "small",
        "medium",
        "large",
        "extra-large"
      ],
      "required": true,
      "default": "small"
    },
    "background_color": {
      "type": "string"
    }
  }
}
