'use strict';

/**
 * agent-landing-hero router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::agent-landing-hero.agent-landing-hero');
