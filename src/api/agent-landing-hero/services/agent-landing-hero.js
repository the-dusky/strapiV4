'use strict';

/**
 * agent-landing-hero service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::agent-landing-hero.agent-landing-hero');
