'use strict';

/**
 * dyn-pg-lrn-sub-pg service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::dyn-pg-lrn-sub-pg.dyn-pg-lrn-sub-pg');
