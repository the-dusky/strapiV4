'use strict';

/**
 * agent-landing-brochure router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::agent-landing-brochure.agent-landing-brochure');
