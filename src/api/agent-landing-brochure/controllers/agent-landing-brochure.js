'use strict';

/**
 *  agent-landing-brochure controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::agent-landing-brochure.agent-landing-brochure');
