'use strict';

/**
 * agent-landing-brochure service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::agent-landing-brochure.agent-landing-brochure');
