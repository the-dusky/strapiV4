'use strict';

/**
 * agent-landing-intro-row router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::agent-landing-intro-row.agent-landing-intro-row');
