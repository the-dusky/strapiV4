'use strict';

/**
 * international-community service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::international-community.international-community');
