'use strict';

/**
 * ecosystem-roadmap service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::ecosystem-roadmap.ecosystem-roadmap');
