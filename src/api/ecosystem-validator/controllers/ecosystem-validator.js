'use strict';

/**
 *  ecosystem-validator controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::ecosystem-validator.ecosystem-validator');
